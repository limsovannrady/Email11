[project]
name = "tempmail-bot"
version = "0.1.0"
description = "Telegram TempMail Bot"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot[job-queue]>=22.7",
    "requests>=2.32.5",
]
